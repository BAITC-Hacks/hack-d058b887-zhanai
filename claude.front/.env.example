# mock  — расчёт и «AI» работают прямо в браузере, сервер не нужен (режим разработки интерфейса)
# http  — все запросы идут на /api (в dev-режиме Vite проксирует их на http://localhost:8000)
VITE_API_MODE=mock
